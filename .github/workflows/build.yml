name: Build

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main, dev]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Configure git for tests
        run: |
          git config --global user.name "CI"
          git config --global user.email "ci@clanker.zone"
          git config --global init.defaultBranch main

      - uses: actions/setup-java@v4
        with:
          distribution: jetbrains
          java-version: 17

      - uses: gradle/actions/setup-gradle@v4

      - name: Build
        run: ./gradlew build

      - name: Coverage report
        if: always()
        run: ./gradlew koverLog

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: |
            docx-gradle-plugin/build/reports/tests/
            docx-gradle-plugin/build/reports/kover/
            docx-web/build/test-results/
            docx-web/build/reports/tests/
            srcx-gradle-plugin/build/reports/tests/
            srcx-gradle-plugin/build/reports/kover/
            workspace-report-model/build/reports/tests/
            workspace-report-model/build/reports/kover/
          retention-days: 14
